{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); [ -n \"$f\" ] || exit 0; case \"$f\" in *.ts|*.html) npx --no-install eslint --fix \"$f\" 2>/dev/null || true;; esac"
          }
        ]
      }
    ]
  }
}
